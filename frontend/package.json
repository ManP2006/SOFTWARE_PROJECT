{
  "name": "pps-frontend",
  "version": "1.0.0",
  "description": "Payroll system frontend",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "puppeteer": "^25.0.2"
  }
}