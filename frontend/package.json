{
  "name": "pps-frontend",
  "version": "1.0.0",
  "description": "Payroll system frontend",
  "scripts": {
    "dev": "live-server --port=3000 --entry-file=public/index.html .",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "vite": "^8.0.14"
  },
  "dependencies": {
    "puppeteer": "^25.0.2"
  }
}
